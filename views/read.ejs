<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <form action="/read" method="post">
        <div class="bg-zinc-800 max-w-full min-h-screen text-white pt-5 pl-5">
            <a class="text-blue-500 m-1 mt-20" href="/">Go back to home</a>
            <h1 class="text-white text-3xl mb-6 mt-5 tracking-tighter">All users</h1>

            <div>
                    <div class="users flex gap-4 flex-wrap">
                        <% if(users.length > 0) { %>
                        <% users.forEach(function(user) { %>
                        <div class="user w-60 h-50 p-4 bg-zinc-700 rounded-lg">
                            <div class="w-full h-60 bg-red-500 rounded-lg">
                                <% if(user.image && user.image.length > 0) { %>
                                    <img class="w-full h-full object-cover object-top overflow-hidden rounded-lg" src="<%= user.image %>">
                                <% } else { %>
                                    <img class="w-full h-full object-cover object-top overflow-hidden rounded-lg" src="https://static.vecteezy.com/system/resources/previews/009/292/244/non_2x/default-avatar-icon-of-social-media-user-vector.jpg">
                                <% } %>
                        </div>
                        <h3 class="text-white mt-2"><%= user.name %></h3>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="flex gap-5 mt-7">
                            <a class="text-yellow-500" href="/edit/<%= user._id %>">edit user</a>
                            <a class="text-red-500" href="/delete/<%= user._id %>">delete user</a>
                        </div>
                    </div>
                <% }) %>
                <% } else { %>
                    <h2 class="text-zinc-500">No users yet</h2>
                <% } %>
                </div>

            </div>
            

        </div>
            
    </form>
    
</body>
</html>